{
  "name": "@workspace/subgraph",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:testnet": "goldsky subgraph deploy vemezo-auto-compounder/1.0.0 --path .",
    "deploy:local": "graph deploy --node http://localhost:8020/ vemezo-auto-compounder"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
